import { Box } from "@mui/material";
import React, { Component } from "react";

export default function Header() {
  return (
    <Box
      sx={{
        minHeight: "5rem",
        width: "100%",
        background: "#f5f5f5",
      }}></Box>
  );
}
